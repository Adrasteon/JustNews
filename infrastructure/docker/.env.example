# JustNews Docker Environment Configuration
# Copy this file to .env and update the values for your deployment

# Database Configuration
POSTGRES_PASSWORD=change_me_secure_password
REDIS_PASSWORD=change_me_secure_password

# Grafana Configuration
GRAFANA_ADMIN_USER=admin
GRAFANA_ADMIN_PASSWORD=change_me_secure_password

# Deployment Environment
DEPLOY_ENV=development
LOG_LEVEL=INFO

# Optional: External URLs (if deploying behind reverse proxy)
# EXTERNAL_URL=http://localhost
# MCP_BUS_EXTERNAL_URL=http://localhost:8000